apiVersion: v1
kind: Service
metadata:
  name: site-web-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: site-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
